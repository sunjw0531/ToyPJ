import { FC } from 'react';
import styled from 'styled-components';

const HeaderContainer = styled.div`
  position: absolute;
  top: 0;
  box-shadow: 0px 0px 5px 0px;
  width: 100vw;
`;

const Header: FC = () => {
  return (
    <>
      <HeaderContainer>asdfasdfasd</HeaderContainer>
    </>
  );
};

export default Header;
